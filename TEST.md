# TruthPulse — テスト手順

ビルドが通ったので、動作確認の順序をまとめます。
**シミュレータで 8 割確認 → 実機で残り 2 割**という進め方が効率的です。

---

## パート 1: シミュレータ（⌘R、5 分）

デバイス選択で `iPhone 17 Pro` などを選んで ⌘R。

### 1-1. モード選択画面

| 見るところ | 期待する動き |
|---|---|
| ロゴ | 3 重のリングが外側へ広がって消える。約 0.87 秒間隔で次のリングが出る |
| ソロのカード | 選択中なので Cyan の枠が光り、背景がわずかに青い |
| デュオのカード | タップすると選択が移り、枠の光もそちらへ移動 |
| ボタンの文言 | ソロ選択時「スキャンを開始」/ デュオ選択時「2人でスキャン」に変わる |
| 履歴カード | 初回は「まだ記録がありません」でグレーアウト、タップ不可 |

### 1-2. ソロ・スキャン

1. 「スキャンを開始」→ **質問は任意**。空のままでも進めます
   （第三者が口頭で質問する使い方を想定しています）
2. ボタンを押す → タッチパッドが出る
3. **パッドに指（マウス）を置いたまま 8 秒**

| 見るところ | 期待する動き |
|---|---|
| スコアリング | 数字が二段の脈動でわずかに拡大縮小 |
| 波形 | 心電図風のスパイクが右から左へ流れる |
| 走査ライン | パッド内を上下に往復 |
| メトリクス | 心拍 / 微振動 / 緊張度 が揺れながら変化 |
| カウンタ | `計測中 04.2秒 / 08.0秒` が進む |

**動かしたときの挙動も確認してください:**

- **押したままドラッグして大きく動かす** → スコアが下がる（動揺量として拾っています）
- **途中で指を離す** → 「指が離れました」に変わり、**時間が止まる**
- **3 秒以内に戻す** → 止まった位置から再開
- **3 秒放置** → 最初からやり直し

### 1-3. 結果画面

8 秒経つと「解析中…」→ 結果画面がせり上がります。

| 見るところ | 期待する動き |
|---|---|
| 見出し | **日本語で強度つき**。「まちがいなく本当」「たぶん嘘」「まっ赤な嘘」など 6 段階 |
| バッジ | 小さい状態から弾んで出現、同時にリングが 1 回だけ広がって消える |
| 内訳バー 4 本 | 上から順に 0.12 秒ずつ遅れて伸びる |
| 色 | 「心拍の安定性」だけ他と色の意味が逆（高い＝ミント） |
| 質問なしの場合 | 質問行が出ず、判定・スコア・内訳だけになる |
| シェア | 右上のアイコン → 画像が生成される。**「画像を保存」で写真に入れて 1080×1350px か確認** |

「もう一問」で同じ質問のままやり直し、「終了」でモード選択に戻ります。

### 1-4. 履歴

モード選択に戻ると「今夜のスコア 1件」に変わっています。
タップ → 一覧 → 行をタップで結果画面が再表示（「もう一問」ボタンは出ません）。

### 1-5. デュオ（シミュレーション動作）

シミュレータにはカメラがないので、**2 つのダミー顔が自動で近づく**モードで動きます。

1. デュオを選択 →「2人でスキャン」→ 質問は任意 →「カメラを起動」
2. 「シミュレータではカメラが使えないため…」の注意書きが出ていれば正常

| タイミング | 期待する表示 |
|---|---|
| 起動直後 | `顔を検出中 0/2` → `2/2` |
| 顔が離れている | `位置合わせ`、枠はマゼンタとシアン |
| 近づいてくる | 同期率% が上昇、リンクラインが太く明るくなる |
| 同期率 72% 到達 | `固定中 0%→100%`、**両方の枠が緑に変わる** |
| 1 秒保持後 | `スキャン中`、下のバーがスキャン進捗に切り替わる |
| 6 秒後 | 解析 → 結果画面 |

ダミーは 5 秒かけて寄るので、放置していれば自動で最後まで進みます。

---

## パート 2: 実機（触覚とカメラ）

シミュレータでは確認できない 2 つを見ます。

### 2-1. 触覚フィードバック

**iPhone のサイレントスイッチをオフ（＝音が鳴る側）にしてください。**
消音中は触覚が弱くなる設定の端末があります。

| 場面 | 期待する触覚 |
|---|---|
| モードカードをタップ | 軽いセレクション感 |
| 計測中（8 秒間） | **心拍に合わせてトン、トンと連続**。画面のスコア脈動と同期しているか |
| 指を離した瞬間 | 警告のダブルパルス |
| 判定確定 | 「本当」なら成功のパターン、「嘘」なら警告のパターン |
| デュオのロック成立 | 軽いセレクション感 |

心拍触覚が**画面の脈動とずれて感じる**場合は教えてください。UIKit の generator に切り替えたばかりなので、タイミングの詰めが必要かもしれません。

### 2-2. デュオ・顔認識（本番）

1. デュオ →「カメラを起動」→ **カメラの許可を「許可」**
2. インカメが起動し、自分の顔に枠が付く

**1 人で試す場合:** 鏡や、もう 1 台のスマホに顔写真を表示して並べても検出されます。

| 確認項目 | 見るところ |
|---|---|
| 枠の追従 | 顔を動かしたとき、枠が遅れずについてくるか |
| 左右反転 | 自分が右に動いたとき、画面上も右に動くか（鏡像表示にしています） |
| 2 人検出 | `顔を検出中 2/2` になるか |
| 同期率の感度 | **どのくらい近づくとロックするか**。ここが今回いちばん見たい点です |
| 中断と再開 | スキャン中に離れて `信号ロスト` → 近づけ直して再開するか |
| 発熱 | 1 分ほど回して端末が熱くならないか（3 フレームに 1 回だけ解析しています） |

---

## 教えてほしいこと

実機で触ったあと、この 3 点だけ教えていただければ調整します。

1. **同期率のロックが「早すぎる / 遅すぎる / ちょうどいい」**
   → `DuoSessionViewModel.swift` の `lockThreshold`（現在 0.72）を動かします
2. **計測時間 8 秒 / 6 秒が「長い / 短い」**
   → `TPTheme.swift` の `samplingDuration` / `duoSamplingDuration`
3. **心拍の触覚が「強い / 弱い / ずれている」**
   → `TPHaptics.swift` の `intensity`（現在 0.55）

---

## 既知の仕様（バグではありません）

- **判定は「本当」か「嘘」の 2 値だけです。** 「もう一度計測」のような宙ぶらりんな結果は出ません。50 点が境界で、そこからの離れ具合を言葉の強さで表しています
- **同じ質問だと似たスコアが出ます。** 質問文のハッシュを判定に 30% 混ぜているためです。「再現性のある機械」に見せる意図的な設計です。**質問を空にすると毎回ランダム**になります
- **アプリアイコンがありません。** 開発中は白いアイコンで表示されます
- **無料の Apple ID だと 7 日で起動できなくなります。** Xcode から ⌘R で入れ直せば再び 7 日使えます
- **ダッシュボード（グラフ付き）と Dynamic Island / ウィジェットは未実装です**
