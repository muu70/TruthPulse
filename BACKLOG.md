# 残りの実装タスク

Claude Code で進めるためのバックログです。上から順に着手する想定で並べています。

作業前に必ず [CLAUDE.md](CLAUDE.md) を読んでください。特に「踏んだ地雷」の章は、
過去にビルドを止めた・表示を壊した実例なので、同じ書き方をすると再発します。

**共通のルール**

- 1 タスク＝1 コミット。コミットメッセージは日本語で「なぜそうしたか」を含める
- 実装後は `bash diagnose.sh` を通し、エラー 0・型チェック 300ms 超 0 を確認する
- 新しい画面もアクセシビリティ（Reduce Motion / Dynamic Type AX / VoiceOver）に対応する
- 画像・音源アセットを外部から持ち込まない。必要なら `tools/` にジェネレータを足す

---

## 1. ダッシュボード（優先度 高）

`SessionHistoryView` を置き換えます。モックは
[mockups/TruthPulse_UI_Mockup.html](mockups/TruthPulse_UI_Mockup.html) の 05 番。

**やること**

- 時間帯別の誠実度グラフ（Swift Charts ではなく `Shape` か `Canvas` で描く。
  Charts は型チェックが重く、この構成だとビルド時間に響く）
- サマリー: 平均誠実度 / 嘘の判定数 / 計測回数 / モード別の内訳
- 「今夜」と「すべて」の切り替え
- 既存の行タップ →`ResultView`（`onRetry` なし）は維持する

**受け入れ条件**

- 記録 0 件でも落ちず、空状態が出る
- 記録 100 件でもスクロールが引っかからない
- グラフに VoiceOver 用の要約ラベルが付いている（`accessibilityLabel` で
  「21時台の平均は64点」のように読み上げる）

**注意**

`ScanSession.startedAt` で時間帯を集計します。`mode` が `.solo / .duo / .pair`
の 3 種になっているので、モード別集計を足すなら `switch` の網羅性に注意。

---

## 2. 合コンモード（優先度 高・課金なしで先に作る）

モックは [mockups/TruthPulse_GokonMode.html](mockups/TruthPulse_GokonMode.html)。
**課金は入れません。** まず遊びとして成立するか確かめるためです。

**やること**

- メンバー設定（人数 2〜8、名前は任意）
- お題デッキ（4 カテゴリ × 30 問 = 120 問。`Resources` ではなく Swift の配列で持つ）
- 順番に計測 → 途中経過 → 全員終了でランキング発表
- 罰ゲームルーレット（20 種）

**データモデル**

`ScanSession` に `groupID: UUID?` と `playerIndex: Int` と `playerName: String`
を足すのが最小の変更です。SwiftData は追加プロパティの軽量マイグレーションに
対応しているので、既定値を与えれば既存データは壊れません。

**受け入れ条件**

- 4 人・質問なしで最後まで通せる
- 途中で中断してもクラッシュしない（アプリを閉じて戻ってきたら最初から、で可）
- ランキングのシェア画像が出せる（`ShareCardView` を流用ではなく別レイアウトで）

**罰ゲームの中身に制約があります**

飲酒を強いるもの（一気飲み等）は入れないでください。実害があるうえ、
App Store の年齢制限が 4+ から 17+ に上がります。
「隣の人のいいところを 3 つ言う」のような、場が続く方向のものにします。

---

## 3. 一斉スキャン（優先度 中）

合コンモードの計測を「1 人ずつ」から「全員同時」に変えます。
順番制だと 1 人 25〜30 秒かかり、待っている人が退屈するためです。

**やること**

- 画面を人数分に区切り、各自が自分のエリアに指を置く
- **同時に置けるのは 4 本まで**。5 人以上は前半・後半の 2 ヒートに分ける
- 計測 5 秒。全員分の結果が一度に出る

**実装のヒント**

`PairSession/MultiTouchPad.swift` がそのまま使えます。返ってくる座標配列を
エリアごとに割り当てるだけです。指が足りない・多いときの扱いは
`PairSessionViewModel.updateTouches` の分岐が参考になります。

---

## 4. アプリ内課金（優先度 低・3 まで終えてから）

合コンモードを ¥500 の非消耗型で解放します。**遊びとして成立することを
確認してから**着手してください。売れない機能に課金を実装しても意味がありません。

**やること**

- StoreKit 2（`Product.purchase()` と `Transaction.currentEntitlements`）
- StoreKit Configuration ファイルを作る（App Store Connect に登録しなくても
  シミュレータで購入フローを試せる）
- 無料枠: 3 人まで・1 回だけ通しで遊べる
- ペイウォールは **1 回遊び終えて 2 回目を始めようとした瞬間**に出す。
  起動直後には絶対に出さない

**必須要件**

- 「購入を復元」ボタン（ガイドライン 3.1.1。無いとリジェクトされます）
- App Store Connect で有料アプリ契約と銀行口座の登録が先に必要

---

## 5. Dynamic Island / ウィジェット（優先度 低）

- ActivityKit で計測の残り時間とライブスコアを表示
- WidgetKit で直近のセッション概要

画面を伏せても「計測が続いている」感を維持するのが狙いです。
Widget Extension を足すとターゲットが 2 つになるため、
`project.pbxproj` を手で編集する必要があります（現在は同期グループ方式）。

---

## 6. App Store 提出（実装ではないが残タスク）

手順は [RELEASE.md](RELEASE.md)。

- [ ] Bundle Identifier を `com.example.TruthPulse` から変更する
- [ ] スクリーンショット 5 枚（iPhone 6.9 インチ / 1320×2868）
- [ ] Apple Developer Program に登録（年額 99 USD）

---

## 実機で確認して調整したい値

まだ実機で詰めていない定数です。触ってみて違和感があれば
[CLAUDE.md](CLAUDE.md) の「調整しやすい定数」の表から変更してください。

| 何を | 現在値 | 判断の仕方 |
|---|---|---|
| ペアの触れ合い距離 | 46pt | 2 人で試して、触れる直前に 100% になるか |
| デュオのロック閾値 | 0.72 | 顔を寄せてすぐロックするか、寄せすぎないと入らないか |
| 心拍の触覚の強さ | 0.55 | 8 秒間続けて、うるさくないか |
| 計測時間 | 8 / 6 / 8 秒 | 緊張が持続するか、間延びしないか |
